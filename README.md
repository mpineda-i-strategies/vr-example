# Example the VR in the browser


## Como interactuar.

* Use las teclas `W` para zoom+ y `S` para zoom- y las letras `A` y `D` para los lados.
* Use el mause y click y arraste para navegar por el la realidad virtual.